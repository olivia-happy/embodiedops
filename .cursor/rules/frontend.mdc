---
description: 前端 TypeScript / React 代码约定（Next.js 15 + React 19）
globs: "frontend/src/**/*.{ts,tsx}"
---

# 前端约定

## 结构

```
frontend/src/
  app/          # Next.js App Router 页面
  components/   # 展示与交互组件
  hooks/        # 数据请求与状态（TanStack Query）
  lib/          # API 客户端与工具
frontend/tests/ # 42 个 Vitest 用例
```

## 硬性要求

1. **诊断结论必须带证据入口。** 任何显示「失败阶段 / 候选机制 / 数值主张」的地方，
   都必须能点开对应的证据抽屉（事件 ID、原始摘要）。不要只呈现结论。
2. **降级状态必须可见。** 后端返回降级 / `needs_evidence` / 契约未填充时，
   前端必须原样呈现这个状态，**不允许用默认值或占位文案把它掩盖掉**。
3. **不在组件里硬编码业务状态语义。** 状态码、阶段名、严重度映射统一放 `lib/`。
4. **合成数据的标注必须显示。** 标注为合成的指标，界面上不能伪装成真实观测。
5. **类型与后端契约手工同步。** 改 `lib/` 的请求 / 响应类型时，
   必须同步检查后端对应的 `*_models.py`（本项目没有自动生成，靠人守）。

## 改完必跑

```bash
cd frontend
npm test          # vitest run
npm run lint      # eslint .
npm run build     # 确认类型与构建通过
```

## 常见坑

- **后端与前端分属两个进程**：本地调试需要同时起后端与 `npm run dev`，
  或直接用 `./scripts/verify_local.ps1`。
- **TanStack Query 的缓存键**：改动查询键会导致下拉 / 切页拿到旧数据，改之前先搜有没有别处依赖。
- **不要在前端做「证据合理性判断」**：前端只负责呈现后端给出的证据与状态，
  判断逻辑留在后端规则层。
- **demo 只读模式**：演示态下界面不应出现任何可修改数据的入口。

## 不要做

- ❌ 不要为了界面好看而隐藏降级 / 未填充状态。
- ❌ 不要把合成指标渲染成真实指标。
- ❌ 不要在组件里重建业务规则（那是后端的事）。
