---
description: 后端 Python 代码约定（FastAPI / DuckDB / 具身诊断核心）
globs: "backend/**/*.py"
---

# 后端约定

## 分层

```
backend/signalforge/
  api/          # FastAPI 路由层——只做参数校验、调用 service、序列化
  services/     # 业务服务层——编排、事务边界
  core/         # 通用能力（配置、检索、生成）
  db/           # DuckDB 接入与仓储基类
  embodied/     # 具身诊断核心：episode 模型、五阶段切分、指标、诊断
  etl/          # 数据导入与转换
backend/tests/  # 266 个 pytest 用例
```

**跨层规则**：路由层不写业务逻辑；逻辑放 services；数据访问统一走 `db/` 的仓储。

## 硬性要求

1. **诊断链条上的任何数值都必须可追溯到事件 ID。** 不允许「模型觉得大概是 XX」这类结论。
2. **fail-closed**：证据不足时返回 `needs_evidence` 或抛错，不要兜一个默认值糊过去。
3. **确定性优先**：阶段切分、评分、排序这类逻辑必须是纯函数式的确定性规则。
   任何引入随机性、时间依赖、模型调用的改动都要先说明理由。
4. **降级显式**：模型 / 数据源不可用时，输出中必须带降级标记，且前端能读到。
5. **demo 数据只读**：`data/` 下的演示数据带 SHA-256 manifest 校验，不要写入或覆盖。
6. **类型注解**：新增函数补全注解；Pydantic 模型集中在对应的 `*_models.py`。

## 改完必跑

```bash
cd backend
pytest -q                     # 全部用例
pytest tests/test_public_artifact_safety.py -q   # 涉及对外内容时
ruff check .
```

## 常见坑

- **`pytest` 必须在 `backend/` 目录下运行**——`pyproject.toml` 的 `testpaths=["tests"]` 依赖这一点。
- **DuckDB 文件不要提交**：`data/*.duckdb`、`data/*.duckdb.wal`、`data/embodied/raw/` 均已 gitignore。
- **`h5py` 用于 RoboMimic 轨迹导入**——处理这些文件时注意资源释放，测试里有对应夹具。
- **不要在 `api/` 里 import `etl/`**：导入链路属于离线侧，不应出现在请求路径上。
- **`test_public_artifact_safety.py` 失败通常意味着真的泄露了东西**，不是测试太严——
  先找泄露源，不要放宽断言。

## 已知未完成项（不要误判为 bug）

- `docs/EMBODIEDOPS_REAL_DATA_CARD.md` 描述的**真实数据契约尚未被填充**，这是预期状态。
- 人工接受率 `manual_review_status=not_performed`，这是预期状态。
- 业务指标模块产出的是**合成**数据，接口上已有标注，不需要「修」。
